<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.babybus.stu_eval.mapper.StuCompMapper">
    <insert id="insert" parameterType="com.babybus.model.StuSum">
        INSERT INTO agilesoftware (username, password,name,idCard,phone,email) VALUES (#{username}, #{password},#{name}, #{idCard},#{phone}, #{email})
    </insert>

    SELECT * FROM StuSum
</mapper>